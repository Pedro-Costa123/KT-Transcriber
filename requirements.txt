# Core transcription
faster-whisper>=1.1.0
python-dotenv>=1.0.1

# Windows GPU runtime for current CTranslate2 (CUDA 12 + cuDNN 9).
# These stay inside the virtualenv; no full CUDA Toolkit install is required.
nvidia-cublas-cu12; sys_platform == "win32"
nvidia-cudnn-cu12; sys_platform == "win32"
